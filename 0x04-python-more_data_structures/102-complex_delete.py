#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    iskey = False
    for llave, val in a_dictionary:
        if llave == key:
            iskey = True

    if iskey:
        del a_dictionary[key]

    return a_dictionary